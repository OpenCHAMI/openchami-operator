---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: openchamiclusters.openchami.openchami.org
spec:
  group: openchami.openchami.org
  names:
    kind: OpenCHAMICluster
    listKind: OpenCHAMIClusterList
    plural: openchamiclusters
    singular: openchamicluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .status.managedByVersion
      name: Operator
      type: string
    - jsonPath: .status.certExpiryTime
      name: CertExpiry
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenCHAMICluster is the Schema for the openchamiclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenCHAMIClusterSpec defines the desired state of an OpenCHAMI
              service cluster.
            properties:
              clusterName:
                description: |-
                  ClusterName is the logical name used in namespaces, Vault paths, and labels.
                  Immutable after creation.
                maxLength: 32
                minLength: 1
                pattern: ^[a-z][a-z0-9-]*$
                type: string
              database:
                description: DatabaseSpec configures the CloudNativePG PostgreSQL
                  cluster.
                properties:
                  backupEnabled:
                    description: BackupEnabled enables CNPG WAL archiving to the object
                      storage bucket.
                    type: boolean
                  instances:
                    default: 3
                    format: int32
                    maximum: 5
                    minimum: 1
                    type: integer
                  storageClass:
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 20Gi
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              domain:
                description: Domain is the external FQDN for the cluster gateway.
                type: string
              logging:
                description: |-
                  LoggingSpec configures legendary-funicular log collection infrastructure.
                  The operator provisions the S3 bucket and deploys the collector DaemonSet.
                  Log schema and query patterns are defined by legendary-funicular and the
                  services — not by the operator.
                properties:
                  enabled:
                    default: true
                    type: boolean
                  flushIntervalSeconds:
                    default: 60
                    description: FlushIntervalSeconds controls how often the collector
                      writes Parquet.
                    format: int32
                    type: integer
                  includeServices:
                    description: |-
                      IncludeServices limits collection to named services.
                      Empty means collect from all services in the namespace.
                    items:
                      type: string
                    type: array
                  logBucket:
                    description: |-
                      LogBucket is the S3 bucket for collected logs.
                      Defaults to "{clusterName}-logs".
                    type: string
                  retentionDays:
                    default: 90
                    description: RetentionDays is how long Parquet objects are kept.
                    format: int32
                    type: integer
                type: object
              networkProbe:
                description: NetworkProbeSpec configures the network reachability
                  probe DaemonSet.
                properties:
                  bmcNetwork:
                    description: |-
                      BMCNetwork describes the BMC network to probe.
                      Required when Enabled=true and Magellan is enabled.
                    properties:
                      subnet:
                        description: Subnet is the CIDR to check for a local route.
                        type: string
                      validateHost:
                        description: ValidateHost is an IP/hostname to TCP-dial as
                          a secondary reachability check.
                        type: string
                      validatePort:
                        description: ValidatePort is the TCP port for the connectivity
                          check.
                        format: int32
                        type: integer
                      validateTimeout:
                        description: ValidateTimeout is the dial timeout, e.g. "3s".
                        type: string
                    required:
                    - subnet
                    type: object
                  enabled:
                    default: false
                    type: boolean
                  intervalSeconds:
                    default: 300
                    description: IntervalSeconds between successive probes on each
                      node.
                    format: int32
                    type: integer
                  provisionNetwork:
                    description: |-
                      ProvisionNetwork describes the provision/PXE network to probe.
                      Required when Enabled=true and CoreDHCP is enabled.
                    properties:
                      subnet:
                        description: Subnet is the CIDR to check for a local route.
                        type: string
                      validateHost:
                        description: ValidateHost is an IP/hostname to TCP-dial as
                          a secondary reachability check.
                        type: string
                      validatePort:
                        description: ValidatePort is the TCP port for the connectivity
                          check.
                        format: int32
                        type: integer
                      validateTimeout:
                        description: ValidateTimeout is the dial timeout, e.g. "3s".
                        type: string
                    required:
                    - subnet
                    type: object
                type: object
              networking:
                description: NetworkingSpec configures the external gateway.
                properties:
                  gatewayClass:
                    default: envoy
                    type: string
                  tls:
                    description: TLSSpec configures TLS certificate management.
                    properties:
                      issuer:
                        default: vault-pki-issuer
                        type: string
                      secretName:
                        description: SecretName defaults to "{clusterName}-gateway-tls".
                        type: string
                    type: object
                type: object
              observability:
                description: ObservabilitySpec configures observability integrations.
                properties:
                  prometheusOperator:
                    description: |-
                      PrometheusOperator enables ServiceMonitor creation.
                      Requires prometheus-operator CRDs to be installed.
                    type: boolean
                type: object
              operatorChannel:
                default: stable
                description: |-
                  OperatorChannel controls reconcile behaviour.
                  "stable" — always reconcile.
                  "pinned" — only reconcile if PinnedVersion matches the running operator version.
                enum:
                - stable
                - pinned
                type: string
              pinnedVersion:
                description: PinnedVersion is the exact operator semver required when
                  OperatorChannel=pinned.
                type: string
              platform:
                description: PlatformSpec references the external infrastructure this
                  cluster depends on.
                properties:
                  objectStorage:
                    description: |-
                      ObjectStorageSpec configures access to the VersityGW S3-compatible object store.
                      VersityGW is never deployed by this operator.
                    properties:
                      bucket:
                        description: Bucket for boot images. Defaults to "{clusterName}-boot-images".
                        type: string
                      endpoint:
                        type: string
                      tlsInsecure:
                        description: TLSInsecure disables TLS certificate verification
                          (dev/test only).
                        type: boolean
                    required:
                    - endpoint
                    type: object
                  vault:
                    description: |-
                      VaultSpec configures access to the external Vault instance.
                      Vault is never deployed by this operator.
                    properties:
                      address:
                        type: string
                      appRoleSecretRef:
                        description: |-
                          AppRoleSecretRef names a Secret containing role_id and secret_id.
                          Required when AuthMethod is appRole.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      authMethod:
                        default: kubernetes
                        description: VaultAuthMethod selects how the operator authenticates
                          to Vault.
                        enum:
                        - kubernetes
                        - appRole
                        type: string
                      caBundleSecretRef:
                        description: CABundleSecretRef names a Secret with key ca.crt
                          for Vault TLS verification.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - address
                    type: object
                required:
                - objectStorage
                - vault
                type: object
              services:
                description: ServicesSpec configures all operator-managed services.
                properties:
                  bootService:
                    description: BootServiceSpec configures the boot-service.
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      replicas:
                        default: 2
                        description: Replicas is ignored for DaemonSet and CronJob
                          services.
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  coreDHCP:
                    description: CoreDHCPSpec configures the CoreDHCP DaemonSet.
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      knownLeaseDuration:
                        default: 1h
                        type: string
                      leaseRanges:
                        description: |-
                          LeaseRanges defines DHCP subnet ranges to serve.
                          At least one range is required when Enabled=true (validated by admission webhook).
                        items:
                          description: DHCPLeaseRange defines a DHCP subnet range
                            to serve.
                          properties:
                            end:
                              type: string
                            start:
                              type: string
                            subnet:
                              type: string
                          required:
                          - end
                          - start
                          - subnet
                          type: object
                        type: array
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: |-
                          NodeSelector is used when spec.networkProbe.enabled is false.
                          When network probing is enabled this field is ignored; the operator
                          generates the selector from probe-applied node labels.
                          When probing is disabled this field is required.
                        type: object
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      tolerations:
                        items:
                          description: |-
                            The pod this Toleration is attached to tolerates any taint that matches
                            the triple <key,value,effect> using the matching operator <operator>.
                          properties:
                            effect:
                              description: |-
                                Effect indicates the taint effect to match. Empty means match all taint effects.
                                When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                              type: string
                            key:
                              description: |-
                                Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                              type: string
                            operator:
                              description: |-
                                Operator represents a key's relationship to the value.
                                Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                Exists is equivalent to wildcard for value, so that a pod can
                                tolerate all taints of a particular category.
                                Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                              type: string
                            tolerationSeconds:
                              description: |-
                                TolerationSeconds represents the period of time the toleration (which must be
                                of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                it is not set, which means tolerate the taint forever (do not evict). Zero and
                                negative values will be treated as 0 (evict immediately) by the system.
                              format: int64
                              type: integer
                            value:
                              description: |-
                                Value is the taint value the toleration matches to.
                                If the operator is Exists, the value should be empty, otherwise just a regular string.
                              type: string
                          type: object
                        type: array
                      unknownLeaseDuration:
                        default: 5m
                        type: string
                    type: object
                  magellan:
                    description: MagellanSpec configures the Magellan BMC discovery
                      CronJob.
                    properties:
                      bmcSubnet:
                        description: |-
                          BMCSubnet is the CIDR range to scan for BMC endpoints.
                          Required when Enabled=true (validated by admission webhook).
                        type: string
                      concurrencyPolicy:
                        default: Forbid
                        description: |-
                          ConcurrencyPolicy describes how the job will be handled.
                          Only one of the following concurrent policies may be specified.
                          If none of the following policies is specified, the default one
                          is AllowConcurrent.
                        enum:
                        - Allow
                        - Forbid
                        - Replace
                        type: string
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector is used when spec.networkProbe.enabled
                          is false.
                        type: object
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      schedule:
                        default: '*/30 * * * *'
                        type: string
                    type: object
                  metadataService:
                    description: MetadataServiceSpec configures the metadata (cloud-init)
                      service.
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      replicas:
                        default: 2
                        description: Replicas is ignored for DaemonSet and CronJob
                          services.
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  smd:
                    description: SMDSpec configures the SMD (State Management Database)
                      service.
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      replicas:
                        default: 2
                        description: Replicas is ignored for DaemonSet and CronJob
                          services.
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  tokensmith:
                    description: TokensmithSpec configures the tokensmith OIDC token
                      service.
                    properties:
                      enabled:
                        default: true
                        type: boolean
                      image:
                        description: ImageSpec overrides the default container image
                          for a service.
                        properties:
                          pullPolicy:
                            description: PullPolicy describes a policy for if/when
                              to pull a container image
                            enum:
                            - Always
                            - Never
                            - IfNotPresent
                            type: string
                          repository:
                            type: string
                          tag:
                            type: string
                        type: object
                      oidcIssuerURL:
                        description: OIDCIssuerURL is required when OIDCProvider is
                          "external".
                        type: string
                      oidcProvider:
                        default: vault
                        description: |-
                          OIDCProvider selects the upstream OIDC token source.
                          "vault"    — use Vault's identity/oidc engine as the issuer.
                          "external" — use an existing OIDC provider; OIDCIssuerURL required.
                        enum:
                        - vault
                        - external
                        type: string
                      replicas:
                        default: 2
                        description: Replicas is ignored for DaemonSet and CronJob
                          services.
                        format: int32
                        maximum: 10
                        minimum: 1
                        type: integer
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                type: object
            required:
            - clusterName
            - domain
            - platform
            type: object
          status:
            description: OpenCHAMIClusterStatus defines the observed state of OpenCHAMICluster.
            properties:
              certExpiryTime:
                description: CertExpiryTime is the RFC3339 timestamp of the soonest-expiring
                  TLS certificate.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              logBucket:
                description: LogBucket is the resolved S3 bucket name for log collection.
                type: string
              managedByVersion:
                description: ManagedByVersion is the operator semver that last reconciled
                  this cluster.
                type: string
              namespace:
                description: Namespace is the Kubernetes namespace created for this
                  cluster.
                type: string
              networkProbe:
                description: NetworkProbe reports probe state for each node.
                properties:
                  nodesWithBMCAccess:
                    description: NodesWithBMCAccess lists node names where the BMC
                      probe passes.
                    items:
                      type: string
                    type: array
                  nodesWithProvisionAccess:
                    description: NodesWithProvisionAccess lists node names where the
                      provision/PXE probe passes.
                    items:
                      type: string
                    type: array
                  probeReady:
                    description: |-
                      ProbeReady is true when the DaemonSet is running and at least one node
                      passes each configured probe type.
                    type: boolean
                required:
                - probeReady
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: ClusterPhase is the high-level lifecycle phase of an
                  OpenCHAMICluster.
                type: string
              services:
                additionalProperties:
                  description: ServiceStatus reports runtime state for a single operator-managed
                    service.
                  properties:
                    endpoint:
                      type: string
                    message:
                      type: string
                    ready:
                      type: boolean
                  required:
                  - ready
                  type: object
                description: Services reports per-service readiness and internal endpoint.
                type: object
              topologyVersion:
                description: TopologyVersion is the SHA-256 of the current topology
                  ConfigMap content.
                type: string
              vaultPathPrefix:
                description: VaultPathPrefix is the Vault KV path prefix in use.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
